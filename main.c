//Template C to x86 call
#include <stdio.h>
#include <stdlib.h>
#include <windows.h>

extern int asmfunc();

void c_1D_stencil() {
	
}


int main() {
	const size_t ARRAY_SIZE = 1 << 20;
	const size_t ARRAY_BYTES = ARRAY_SIZE * sizeof(float);
	int i;
	int* x, y;
	return 0;
}